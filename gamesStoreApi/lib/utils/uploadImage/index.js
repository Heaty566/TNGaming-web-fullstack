const { uploadAvatar } = require("./user");
const { uploadImageGame } = require("./developer");
const { uploadSliceShow } = require("./admin");

module.exports = { uploadAvatar, uploadImageGame, uploadSliceShow };
