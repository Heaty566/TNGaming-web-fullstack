const { genresSchema } = require("./genres");
const { userSchema } = require("./users");
const { tokenSchema } = require("./token");
const { gameSchema } = require("./games");

module.exports = { genresSchema, userSchema, tokenSchema, gameSchema };
